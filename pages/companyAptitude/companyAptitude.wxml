<view bindtap='previewImage'>
  <block wx:for="{{imageNameList}}" wx:for-item="img" wx:key="i">
    <image class="{{(index+1) % 3 == 0 ? 'middle':''}}" data-index="{{img}}" src="http://oss.kxlist.com/durui/shouquanshu/{{img}}.jpg"></image>
  </block>
</view>
